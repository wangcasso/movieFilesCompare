======================================
  启动说明 - 重要！请阅读
======================================

⚠️ 关于乱码问题的说明：

Windows批处理文件(.bat)在处理中文字符时可能会出现编码问题，
这是因为不同的系统使用不同的默认编码。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 推荐的启动方式（3种）：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方法1: 使用VBScript启动器（推荐）⭐
─────────────────────────────────────
双击运行: 启动程序.vbs

优点: 
• 完美支持中文
• 无编码问题
• 后台运行Python

方法2: 使用命令行
─────────────────────────────────────
1. 按 Win + R
2. 输入: cmd
3. 输入以下命令:
   cd D:\fileCompare
   python video_dedup.py

方法3: 直接运行Python文件
─────────────────────────────────────
直接双击: video_dedup.py

（如果关联了Python，会直接运行）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 已修复的问题：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

之前的 启动程序.bat 和 打包成EXE.bat 使用了UTF-8编码
和中文字符，在某些系统上会显示为乱码。

现在已经改为英文提示，避免编码问题。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 各个启动文件的说明：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

启动程序.vbs     → VBScript启动器（推荐）
启动程序.bat     → 批处理脚本（英文提示）
video_dedup.py   → Python主程序（可直接运行）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 如果你想修复.bat文件的中文显示：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方法A: 用记事本打开.bat文件
  1. 右键 .bat 文件
  2. 选择"编辑"
  3. 另存为
  4. 编码选择 "ANSI" 或 "GBK"
  5. 保存

方法B: 使用PowerShell
  右键.bat文件 → "使用PowerShell运行"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 建议：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

直接使用 启动程序.vbs 或 命令行方式，
这样可以获得最佳的兼容性。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

现在请尝试：
→ 双击 "启动程序.vbs" 启动程序！
